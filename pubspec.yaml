name: hold_labs
description: "A game about temprature science"
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.2.3 <4.0.0"

dependencies:
  flame: 1.12.0
  flutter:
    sdk: flutter

dev_dependencies:
  flame_lint: 1.1.2
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
